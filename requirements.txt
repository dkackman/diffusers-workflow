# diffusers-workflow runtime dependencies
#
# The dependency list lives in pyproject.toml - the single source that the
# wheel, install.sh, and CI all resolve. This file just points pip at it
# (editable install, with the HTTP server extra).
#
# PyTorch from PyPI is CPU-only on Linux. For CUDA, install torch first
# from pytorch.org's index and pip will leave it in place:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#
# Released diffusers may lag the newest model pipelines this project
# targets. For git HEAD, install it on top afterwards:
#   pip install git+https://github.com/huggingface/diffusers
#
-e .[server]
