{
  "id": "GenerateSpeech",
  "description": "Speaks a line of text with a local text-to-speech model and saves it as a wav. The result declares no 'sample_rate': a generated track carries the rate its model produced it at, so saying nothing is what saves it correctly.",
  "variables": {
    "text": "Somebody finished the pistachio and nobody is saying anything.",
    "model_name": "facebook/mms-tts-eng"
  },
  "steps": [
    {
      "name": "speak",
      "task": {
        "command": "generate_speech",
        "arguments": {
          "text": "variable:text",
          "model_name": "variable:model_name"
        }
      },
      "result": {
        "content_type": "audio/wav"
      }
    }
  ]
}
